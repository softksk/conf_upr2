# 📊 Визуализатор графа зависимостей для Ubuntu (apt)

**Автор:** Кирюшин Артём, ИКБО-51-24  
**Вариант:** №11  
**Дисциплина:** Конфигурационное управление

---

## 🎯 Структура проекта по этапам

Проект разбит на **5 этапов**, каждый этап имеет отдельный коммит и файлы:

### Этап 1: Минимальный прототип с конфигурацией
- **Файлы:** `stage1_main.py`, `stage1_config.ini`, `tests/test_stage1.py`
- **Коммит:** "Этап 1: Минимальный прототип с конфигурацией"
- **Демонстрация:**
  ```bash
  python stage1_main.py stage1_config.ini
  python tests/test_stage1.py
  ```

### Этап 2: Сбор данных о зависимостях
- **Файлы:** `stage2_main.py`, `stage2_config_*.ini`, `tests/test_stage2.py`
- **Коммит:** "Этап 2: Сбор данных о зависимостях"
- **Демонстрация:**
  ```bash
  python stage2_main.py stage2_config_test.ini
  python tests/test_stage2.py
  ```

### Этап 3: Построение графа зависимостей
- **Файлы:** `stage3_main.py`, `stage3_config_*.ini`, `tests/stage3_test_repo_cycle.json`
- **Коммит:** "Этап 3: Построение графа зависимостей (BFS + рекурсия)"
- **Демонстрация:**
  ```bash
  python stage3_main.py stage3_config_cycle.ini    # С циклами
  python stage3_main.py stage3_config_filter.ini   # С фильтрацией
  ```

### Этап 4: Дополнительные операции
- **Файлы:** `stage4_main.py`
- **Коммит:** "Этап 4: Дополнительные операции (порядок загрузки)"
- **Демонстрация:**
  ```bash
  python stage4_main.py stage2_config_test.ini
  ```

### Этап 5: Визуализация
- **Файлы:** `stage5_main.py`
- **Коммит:** "Этап 5: Визуализация (PlantUML + ASCII-дерево)"
- **Демонстрация:**
  ```bash
  python stage5_main.py stage2_config_test.ini
  ```

---

## 📂 Структура файлов

```
conf_upr/
├── stage1_main.py              # Этап 1: Конфигурация
├── stage2_main.py              # Этап 2: Сбор данных
├── stage3_main.py              # Этап 3: Построение графа
├── stage4_main.py              # Этап 4: Порядок загрузки
├── stage5_main.py              # Этап 5: Визуализация
│
├── stage1_config.ini           # Конфиг для этапа 1
├── stage2_config_test.ini      # Конфиг для этапа 2 (тест)
├── stage2_config_real.ini      # Конфиг для этапа 2 (реальный)
├── stage3_config_cycle.ini     # Конфиг для этапа 3 (циклы)
├── stage3_config_filter.ini    # Конфиг для этапа 3 (фильтр)
│
├── README_STAGE1.md            # Документация этапа 1
├── README_STAGE2.md            # Документация этапа 2
├── README_STAGE3.md            # Документация этапа 3
├── README_STAGE4.md            # Документация этапа 4
├── README_STAGE5.md            # Документация этапа 5
│
├── tests/
│   ├── test_stage1.py          # Тесты этапа 1
│   ├── test_stage2.py          # Тесты этапа 2
│   ├── stage2_test_repo.json   # Тестовый репозиторий
│   └── stage3_test_repo_cycle.json  # Репозиторий с циклами
│
└── PRESENTATION_GUIDE.md       # Данное руководство
```

---

## 🎤 Инструкция для презентации

### Подготовка

1. Откройте проект в VS Code
2. Откройте терминал: `bash`
3. Переключитесь в директорию проекта: `cd c:/code/conf_upr`

### Демонстрация Этапа 1

```bash
# Показать коммит
git log --oneline | grep "Этап 1"

# Просмотреть файлы этапа
ls stage1*

# Запустить программу
python stage1_main.py stage1_config.ini

# Запустить тесты
python tests/test_stage1.py
```

**Что показывать:**
- ✅ Загрузка конфигурации из INI
- ✅ Валидация всех параметров
- ✅ Обработка ошибок (файл не найден, неверный тип)
- ✅ Вывод всех параметров

### Демонстрация Этапа 2

```bash
# Показать коммит
git log --oneline | grep "Этап 2"

# Тестовый репозиторий
python stage2_main.py stage2_config_test.ini

# Запустить тесты
python tests/test_stage2.py
```

**Что показывать:**
- ✅ Работа с JSON репозиторием (быстро)
- ✅ Парсинг зависимостей
- ✅ Вывод прямых зависимостей

**Опционально (требует интернет):**
```bash
# Реальный репозиторий Ubuntu (долго, ~1-2 минуты)
python stage2_main.py stage2_config_real.ini
```

### Демонстрация Этапа 3

```bash
# Показать коммит
git log --oneline | grep "Этап 3"

# Граф с циклическими зависимостями
python stage3_main.py stage3_config_cycle.ini

# Граф с фильтрацией
python stage3_main.py stage3_config_filter.ini
```

**Что показывать:**
- ✅ Построение графа транзитивных зависимостей
- ✅ Обработка циклов (пометка)
- ✅ Фильтрация пакетов по подстроке
- ✅ Учет max_depth

### Демонстрация Этапа 4

```bash
# Показать коммит
git log --oneline | grep "Этап 4"

# Запустить с простым графом
python stage4_main.py stage2_config_test.ini
```

**Что показывать:**
- ✅ Порядок загрузки зависимостей
- ✅ Топологическая сортировка
- ✅ Последовательность установки пакетов

### Демонстрация Этапа 5

```bash
# Показать коммит
git log --oneline | grep "Этап 5"

# Запустить с визуализацией
python stage5_main.py stage2_config_test.ini

# Просмотреть созданный PlantUML файл
cat stage5_A.puml
```

**Что показывать:**
- ✅ Генерация PlantUML кода
- ✅ ASCII-дерево с Unicode символами
- ✅ Обработка циклов в дереве
- ✅ Сохранение в файл `.puml`

---

## 🔍 Проверка коммитов

```bash
# Показать все коммиты
git log --oneline

# Показать изменения в конкретном коммите
git show <commit-hash>

# Переключиться на конкретный этап
git checkout <commit-hash>

# Вернуться к последнему коммиту
git checkout main
```

---

## ✅ Чек-лист для защиты

### Этап 1
- [ ] Конфигурация через INI файл
- [ ] Валидация всех 7 параметров
- [ ] Обработка ошибок
- [ ] 6 тестов (100% успешность)

### Этап 2
- [ ] Парсинг Packages.gz (без готовых библиотек!)
- [ ] Извлечение прямых зависимостей
- [ ] Работа с тестовым репозиторием (JSON)
- [ ] 6 тестов (100% успешность)

### Этап 3
- [ ] Алгоритм BFS с рекурсией
- [ ] Обработка циклов
- [ ] Фильтрация пакетов
- [ ] Учет max_depth
- [ ] Тестовые репозитории (простой, с циклами)

### Этап 4
- [ ] Топологическая сортировка (алгоритм Кана)
- [ ] Порядок загрузки зависимостей
- [ ] Демонстрация на тестовых репозиториях

### Этап 5
- [ ] PlantUML диаграмма
- [ ] ASCII-дерево
- [ ] Обработка циклов в дереве
- [ ] Сохранение в файл

---

## 📌 Важные моменты для презентации

1. **Запрет на готовые библиотеки** — парсинг Packages вручную (этап 2)
2. **BFS с рекурсией** — явная реализация алгоритма (этап 3)
3. **Обработка циклов** — через множество `visited` (этапы 3, 5)
4. **Постепенное усложнение** — каждый этап добавляет функциональность
5. **Тесты для каждого этапа** — автоматическая проверка корректности

---

## 🎓 Для преподавателя

Каждый этап имеет:
- ✅ Отдельный файл `stageN_main.py`
- ✅ Отдельный коммит с описанием
- ✅ Документацию `README_STAGEN.md`
- ✅ Тесты (для этапов 1-2)
- ✅ Примеры конфигураций

Можно легко переключаться между этапами через `git checkout`.

---

**РТУ МИРЭА, 2025**
